# Phase 1 AIS ingestion worker
FROM python:3.12-slim

WORKDIR /app

RUN useradd --create-home appuser && chown -R appuser /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ ./app/
COPY worker/ ./worker/

USER appuser
CMD ["python", "-m", "worker.main"]
